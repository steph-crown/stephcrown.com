type TestimonialType = {
  name: string
  role: string
  quote: string
  avatarUrl: string
  /** Optional relationship line, e.g. from LinkedIn */
  context?: string
}

export default TestimonialType
